# 📚 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ

## Шаг 1: Подготовка Firebase

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект `kppcarecontrol`
3. Перейдите в **Firestore Database**
4. Если база не создана, создайте её в режиме **production mode**
5. Настройте правила безопасности:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Разрешаем чтение и запись для всех (упрощённый вариант)
    // В продакшене лучше настроить аутентификацию через Firebase Auth
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

⚠️ **Важно**: Эти правила открывают доступ для всех. Для продакшена рекомендуется настроить Firebase Authentication.

## Шаг 2: Загрузка на GitHub

1. Создайте новый репозиторий на GitHub:
   - Зайдите на [github.com](https://github.com)
   - Нажмите "New repository"
   - Имя: `kpp-care-control` (или любое другое)
   - Сделайте репозиторий **Public** или **Private**
   - НЕ добавляйте README, .gitignore или лицензию (они уже есть)

2. Загрузите файлы в репозиторий:

```bash
# Инициализируйте git в папке проекта
cd /path/to/kpp-care-control
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: KPP Care Control app"

# Подключите удалённый репозиторий
git remote add origin https://github.com/ваш-username/kpp-care-control.git

# Загрузите код
git branch -M main
git push -u origin main
```

## Шаг 3: Деплой на Vercel

1. Откройте [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите репозиторий `kpp-care-control`
5. Настройки оставьте по умолчанию:
   - Framework Preset: **Other**
   - Root Directory: `./`
   - Build Command: (оставить пустым)
   - Output Directory: (оставить пустым)
6. Нажмите **"Deploy"**
7. Ждите 1-2 минуты

## Шаг 4: Проверка работы

После успешного деплоя:

1. Vercel покажет URL вашего приложения (например: `https://kpp-care-control.vercel.app`)
2. Откройте этот URL в браузере
3. Попробуйте войти как админ:
   - Логин: `admin`
   - Пароль: `Kpp2026cc`
4. Создайте тестового пользователя:
   - Выйдите из админ-панели
   - Введите любой логин и пароль
   - Проверьте функционал

## Шаг 5: Обновление приложения

Когда нужно внести изменения:

```bash
# Внесите изменения в код
# Сохраните файлы

# Добавьте изменения в git
git add .
git commit -m "Описание изменений"

# Загрузите на GitHub
git push

# Vercel автоматически пересоберёт и задеплоит новую версию
```

## 🔧 Настройка домена (опционально)

1. В Vercel перейдите в настройки проекта
2. Раздел **Domains**
3. Добавьте свой домен
4. Настройте DNS записи по инструкции Vercel

## 🐛 Возможные проблемы

### Ошибка: "Firebase not defined"
- Проверьте, что Firebase SDK загружается корректно
- Откройте консоль браузера (F12) и посмотрите ошибки

### Ошибка: "Permission denied"
- Проверьте правила Firestore
- Убедитесь, что правила разрешают чтение/запись

### Не сохраняются данные
- Откройте DevTools → Network
- Проверьте запросы к Firestore
- Убедитесь, что конфигурация Firebase правильная

### Не работает localStorage (сессии)
- Проверьте, что браузер не в режиме "Инкогнито"
- Проверьте настройки cookies

## 📱 Использование на мобильных

Приложение адаптивное и работает на мобильных устройствах:

1. Откройте URL в браузере смартфона
2. Добавьте на главный экран (Safari: Share → Add to Home Screen)
3. Приложение будет работать как обычное мобильное приложение

## 🔐 Безопасность в продакшене

Для продакшн-версии рекомендуется:

1. Настроить **Firebase Authentication**
2. Обновить правила Firestore:
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth.uid == userId;
      
      match /history/{historyId} {
        allow read: if request.auth != null;
        allow write: if request.auth.uid == userId;
      }
    }
  }
}
```
3. Хранить пароли в зашифрованном виде (bcrypt)
4. Использовать HTTPS (Vercel делает это автоматически)
